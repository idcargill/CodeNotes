/*
MONGO QUERY SELECTORS
https://docs.mongodb.com/manual/reference/operator/query/#std-label-query-selectors

$eq - equal
$gt - greater than
$gte - greater than equal
$in - in array [ ]
$lt - less than
$lte - less than equal
$ne - not equal
$nin - non of the values specified

LOGICAL
$and
$not
$nor
$or


ELEMENT
$exists
$type

EVALUATION
$expr - aggregation expressions
$jsonSchema - validate against given JSON schema
$mod - modulo operation
$regex - matches regex
$text - performs text search
$where - matches documents that satisfy

GEOSPATIAL
$geoInersects
$geoWithin
$near
$nearSphere


ARRAY
$all - matches arrays that contain all elements
#elemMatch - Selects document if element in the array

...
*/
